%% Options for packages loaded elsewhere
%\PassOptionsToPackage{unicode}{hyperref}
%\PassOptionsToPackage{hyphens}{url}
%%
\documentclass[fontsize=13pt]{scrbook}


\tracinglostchars=3

\RequirePackage{fontspec}

\RequirePackage[document]{ragged2e}

\RequirePackage[none]{hyphenat}

%\RequirePackage{amsmath,amssymb}
%\RequirePackage{lmodern}
%\RequirePackage{iftex}
%\ifPDFTeX
%  \RequirePackage[T1]{fontenc}
%  \RequirePackage[utf8]{inputenc}
%  \RequirePackage{textcomp} % provide euro and other symbols
%\else % if luatex or xetex
%  \RequirePackage{unicode-math}
%  \defaultfontfeatures{Scale=MatchLowercase}
%  \defaultfontfeatures[\rmfamily]{Ligatures=TeX,Scale=1}
%\fi
%% Use upquote if available, for straight quotes in verbatim environments
%\IfFileExists{upquote.sty}{\RequirePackage{upquote}}{}
%\IfFileExists{microtype.sty}{% use microtype if available
%  \RequirePackage[]{microtype}
%  \UseMicrotypeSet[protrusion]{basicmath} % disable protrusion for tt fonts
%}{}
%\makeatletter
%\@ifundefined{KOMAClassName}{% if non-KOMA class
%  \IfFileExists{parskip.sty}{%
%    \RequirePackage{parskip}
%  }{% else
%    \setlength{\parindent}{0pt}
%    \setlength{\parskip}{6pt plus 2pt minus 1pt}}
%}{% if KOMA class
%  \KOMAoptions{parskip=half}}
%\makeatother



%\RequirePackage{xcolor}
\RequirePackage{longtable,booktabs,array}
\RequirePackage{calc} % for calculating minipage widths
%% Correct order of tables after \paragraph or \subparagraph
%\RequirePackage{etoolbox}
%\makeatletter
%\patchcmd\longtable{\par}{\if@noskipsec\mbox{}\fi\par}{}{}
%\makeatother
%% Allow footnotes in longtable head/foot
%\IfFileExists{footnotehyper.sty}{\RequirePackage{footnotehyper}}{\RequirePackage{footnote}}
%\makesavenoteenv{longtable}
\RequirePackage{graphicx}

\makeatletter
\newcommand{\maxwidth}{\ifdim\Gin@nat@width>\linewidth\linewidth\else\Gin@nat@width\fi}
\newcommand{\maxheight}{\ifdim\Gin@nat@height>\textheight\textheight\else\Gin@nat@height\fi}
\makeatother
% Scale images if necessary, so that they will not overflow the page
% margins by default, and it is still possible to overwrite the defaults
% using explicit options in \includegraphics[width, height, ...]{}
\setkeys{Gin}{width=\maxwidth,height=\maxheight,keepaspectratio}
% Set default figure placement to htbp
%\makeatletter
%\def\fps@figure{htbp}
%\makeatother
%\setlength{\emergencystretch}{3em} % prevent overfull lines

\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
%\setcounter{secnumdepth}{-\maxdimen} % remove section numbering
% Default fonts for the document
\defaultfontfeatures{Ligatures=NoCommon}
\setmainfont{Lato}
\setmonofont{PT Mono}
%\ifLuaTeX
%  \RequirePackage{selnolig}  % disable illegal ligatures
%\fi
%\RequirePackage{hyperref}
%\IfFileExists{xurl.sty}{\RequirePackage{xurl}}{} % add URL line breaks if available
%\urlstyle{same} % disable monospaced font for URLs
%\hypersetup{
%  hidelinks,
%  pdfcreator={LaTeX via pandoc}}
%
%\author{}
%\date{}

\RequirePackage[lmargin=10mm,rmargin=10mm,tmargin=15mm,bmargin=15mm]{geometry}

% Hyperlinks colors
\RequirePackage{hyperref}
\hypersetup{bookmarksdepth=0}

\RequirePackage{color}
\RequirePackage[svgnames]{xcolor}
\hypersetup{colorlinks=true, allcolors=Chocolate}


%% Fonts and colors for headings of different levels
%\RedeclareSectionCommand[
%    style=part,
%    beforeskip=80pt,
%    tocbeforeskip=200pt,
%    font=\raggedright \fontspec{Fira Sans} \fontsize{50}{50}\selectfont,
%    tocentryformat=\fontspec{Fira Sans} \fontsize{22}{22}\selectfont,
%    tocpagenumberformat=\normalfont\bfseries,
%  ]{part}
%\RedeclareSectionCommand[
%    style=part,
%    beforeskip=80pt,
%    tocbeforeskip=10pt,
%    font=\raggedright \fontspec{Fira Sans} \fontsize{40}{40}\selectfont,
%    tocentryformat=\fontspec{Fira Sans} \fontsize{18}{18}\selectfont,
%    tocpagenumberformat=\normalfont\bfseries,
%  ]{chapter}
\RedeclareSectionCommand[
    font=\fontspec{Lato} \fontsize{20}{20}\selectfont,
  ]{section}
%\RedeclareSectionCommand[ % #
%    font=\normalfont \fontspec{Fira Sans} \fontsize{32}{32}\selectfont,
%    tocindent=0em,
%  ]{subsubsection}
%\RedeclareSectionCommand[ % ##
%    font=\normalfont \fontspec{Fira Sans} \fontsize{22}{22}\selectfont,
%    beforeskip=20pt,
%    afterskip=1pt,
%    tocindent=0em,
%  ]{paragraph}
%\RedeclareSectionCommand[ % ###
%    font=\normalfont \fontspec{Fira Sans} \fontsize{16}{16}\selectfont,
%    beforeskip=15pt,
%    afterskip=1pt,
%  ]{subparagraph}
